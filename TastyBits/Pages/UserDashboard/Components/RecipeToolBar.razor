@inject NavigationManager navMan

<div class="flex border p-3 items-center rounded-lg">
    <button @onclick="AddNewRecipe" class="primaryOutline">Add new</button>
</div>

@code {
    private void AddNewRecipe()
    {
        navMan.NavigateTo("/dashboard/recipe-new");
    }
}
